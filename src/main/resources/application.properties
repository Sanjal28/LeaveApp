spring.application.name=leaveapp


# CORE SERVER CONFIGURATION
server.port=8080
server.error.include-stacktrace=never

# DATABASE CONFIGURATION (H2 In-Memory for Easy Start)
spring.datasource.url=jdbc:h2:mem:leaveappdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

# JWT SECURITY CONFIGURATION
# IMPORTANT: Generate a secure, long, random secret for production!
# You can use an online generator to create a 64-byte hex string.
jwt.secret=7336763979244226452948404D635166546A576E5A7234753778214125442A47
jwt.expiration.minutes=60

# ===================================================================
# EMAIL NOTIFICATION CONFIGURATION
# ===================================================================
# ? TIP: Use Mailtrap.io for safe email testing in development.
# It captures emails without sending them to real inboxes.
# Sign up for a free account, go to your inbox, and find these credentials.
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=e2b091d84c3ef7
spring.mail.password=09bc31af6de0b4
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
mail.from=no-reply@leaveapp.com

# ===================================================================
# ACTUATOR
# ===================================================================
management.endpoints.web.exposure.include=health,info,prometheus